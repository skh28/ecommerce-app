# Copy to .env and fill in. NextAuth needs a secret for session encryption.
# Generate one with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000

DATABASE_URL="file:./dev.db"
